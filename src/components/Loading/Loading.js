import './loading.scss';

const Loading = {
  render: () => {
    return html`<img
      class="loading"
      src="./src/images/loading.gif"
      alt="Loading"
    />`;
  },
};

export default Loading;
